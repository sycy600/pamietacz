language: python
python:
  - "2.7"
install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - sleep 4
  - pip install --upgrade setuptools
  - python bootstrap.py
  - bin/buildout
script:
  - bin/functional-tests
